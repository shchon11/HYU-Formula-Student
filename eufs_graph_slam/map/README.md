# Saved SLAM maps
Cone maps saved via the graph SLAM `~/save_map` service land here as
`map_<timestamp>.csv`, in the same column format as `eufs_tracks` track CSVs
(tag,x,y,direction,x_variance,y_variance,xy_covariance).
